/**
 * Theme and UI animations.
 * Used in styled components CSS.
 *
 * Keep in sync with CSS.
 */
const animations: any = {
  timing: {
    default: 0.35,
    fast: 0.25,
  },
  easing: {
    default: 'ease-out',
  },
};

export default animations;
