/**
 * Theme and UI colors.
 *
 * Keep in sync with CSS.
 *
 * Tips
 * - Use RGBA values for colors with transparency only.
 * - Apply default transparency of '1' and modify it later in code via helper functions.
 */
const colors: any = {
  brand: {},
  theme: {
    background: '',
  },
  text: {},
  ui: {
    global: {},
    components: {},
  },
};

export default colors;
